<%= form_with(model: availability) do |form| %>
  <% if availability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(availability.errors.count, "error") %> prohibited this availability from being saved:</h2>

      <ul>
        <% availability.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :time_zone %>
    <%= form.datetime_select :time_zone %>
  </div>

  <div class="field">
    <%= form.label :day %>
    <%= form.datetime_select :day %>
  </div>

  <div class="field">
    <%= form.label :day_start %>
    <%= form.time_select :day_start %>
  </div>

  <div class="field">
    <%= form.label :day_end %>
    <%= form.time_select :day_end %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
